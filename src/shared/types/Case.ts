/**
 * Case.ts
 *
 * Discovery 시스템에서 사용하는 Case 관련 타입 정의
 */

/**
 * Discovery 시스템에서 사용하는 Location 타입
 */
export interface Location {
  id: string;
  name: string;
  description: string;
  emoji: string;
}
